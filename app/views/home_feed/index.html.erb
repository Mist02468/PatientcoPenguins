<h1>Home</h1>

<table>
	<% @posts.each do |post|%>
	    <tr>
	      <td><%= post.text %></td>
	      <td><%= post.created_at %></td>
	      <td><%= post.user.firstName %> <%= post.user.lastName %></td>
	    </tr>
	<% end %>
</table>